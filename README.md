# C++ Pointers Example

This repository contains simple C++ programs.

## Prerequisites

- A C++ compiler (e.g., g++)
- C++17 standard support

## How to Run

To compile and run the program, use the following command:

```bash
g++ -std=c++17 file_name.cpp -o pointer_code && ./pointer_code
```


